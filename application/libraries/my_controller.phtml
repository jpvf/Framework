<?php 

class MY_controller extends Controller
{
    var $titulo = '';
    
    function __construct()
    {
        parent::__construct();
    }


    function before()
    {    
		if ( ! $this->session->valid_session() && $this->uri->get(1) != 'login')
		{	
			redirect(get_url() . '/login');
		}
    }

    function after()
    {
        //debug($this->db->get_queries());
    }
    
}